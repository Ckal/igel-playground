name: igel-playground
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/igel-playground
  original_url: https://huggingface.co/spaces/Chris4K/igel-playground

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
